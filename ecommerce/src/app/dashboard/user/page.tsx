import React from 'react';
import UserPage from '@/components/UserPage/UserPage';

const User: React.FC = () => {
    return (
        <main>
            <UserPage />
        </main>
    );
};

export default User;